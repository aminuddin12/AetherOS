# Aether CLI

AetherOS Developer Platform Runtime.

This package provides the `aether` CLI tool, which serves as the central developer OS for managing workspaces, plugins, providers, and inspecting the AetherOS kernel and execution engine.

## Installation

```bash
uv pip install -e .
```

## Usage

```bash
aether --help
```
