[project]
name = "aether-workspace"
version = "1.0.0"
description = "AetherOS Workspace Core Runtime"
authors = [{ name = "Aminuddin Adl", email = "aminuddinadl@aetheros.com" }]
dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
